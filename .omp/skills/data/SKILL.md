---
name: data
description: Analyze and transform data while preserving original datasets and validating invariants.
---

# Data workflow

- Inspect the schema first.
- Never overwrite the original dataset; save results in `output/`.
- Validate row counts, totals, types, and relevant invariants.
- State assumptions explicitly and never remove data silently.
