---
name: documents
description: Create derived documents without modifying immutable source files in incoming.
---

# Document workflow

- Treat `incoming/` as immutable and never overwrite originals.
- Save results under a different filename in `output/`.
- Report the source, output, changes, and validation.
